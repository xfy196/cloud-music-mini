<view class="container">
  <songList bind:handlePlaySong="handlePlaySong" detail="{{detail}}"></songList>
  <miniAudio wx:if="{{audioPlay}}" audioPlay="{{audioPlay}}" playObj="{{playObj}}"></miniAudio>
</view>